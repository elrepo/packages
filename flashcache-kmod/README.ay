Command to build the package:

For EL5:

rpmbuild -bb --target `uname -m` --define 'kversion 2.6.18-274.el5' --define 'dist .el5.elrepo' flashcache-kmod.spec

For EL6:

rpmbuild -bb --target `uname -m` --define 'kversion 2.6.32-220.el6.x86_64' --define 'dist .el6.elrepo' flashcache-kmod.spec
