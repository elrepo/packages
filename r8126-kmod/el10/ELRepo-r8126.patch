diff -Naurp r8126-10.016.00.orig/src/r8126_ptp.c r8126-10.016.00.el10_2/src/r8126_ptp.c
--- r8126-10.016.00.orig/src/r8126_ptp.c	2025-06-13 08:55:26.000000000 -0400
+++ r8126-10.016.00.el10_2/src/r8126_ptp.c	2026-05-22 10:57:06.554084382 -0400
@@ -751,8 +751,12 @@ void rtl8126_ptp_init(struct rtl8126_pri
 
         /* init a hrtimer for pps */
         tp->pps_enable = 0;
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6,13,0) && !(RHEL_MAJOR == 10 && RHEL_MINOR >= 2)
         hrtimer_init(&tp->pps_timer, CLOCK_MONOTONIC, HRTIMER_MODE_REL);
         tp->pps_timer.function = rtl8126_hrtimer_for_pps;
+#else
+        hrtimer_setup(&tp->pps_timer, rtl8126_hrtimer_for_pps, CLOCK_MONOTONIC, HRTIMER_MODE_REL);
+#endif
 
         /* reset the PTP related hardware bits */
         rtl8126_ptp_reset(tp);
