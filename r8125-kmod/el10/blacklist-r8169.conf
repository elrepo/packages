# /usr/lib/modprobe.d/blacklist-r8169.conf provided by http://elrepo.org
#
# If the r8169 driver continues to load, run as root:
# dracut -f /boot/initramfs-$(uname -r).img $(uname -r)

blacklist r8169
